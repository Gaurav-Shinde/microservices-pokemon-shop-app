
server:
  port: 9296
  
spring:
  application:
    name: CONFIG-SERVER
    
---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gaurav-Shinde/microservices-config.git
          clone-on-start: true